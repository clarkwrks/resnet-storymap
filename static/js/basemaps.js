var Basemaps_map = {
	// cont:{},
	init: function(){
		if(typeof map.legend !== 'undefined'){
			map.removeControl(map.legend)
		}
        // var layerControl = L.control.layers(layers)
        // this.cont = map.addControl(new layerControl());
	}


}

